import ContactForm from "./ContactForm";

export const metadata = {
  title: "Contact Support — PartyShare",
  description: "Get help or contact PartyShare support.",
};

export default function ContactForm() {
  return <ContactForm />;
}
